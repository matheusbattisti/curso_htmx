<nav class="bg-gray-800 p-4 text-white">
  <div class="container mx-auto flex justify-between">
    <h1 class="text-xl font-bold">File Sharer</h1>
    <div>
      <% if (typeof userId !== 'undefined' && userId) { %>
      <!-- Usuário está logado -->
      <a href="/admin/all-files" class="text-white hover:text-gray-300"
        >Biblioteca</a
      >
      <a href="/admin" class="text-white hover:text-gray-300 ml-4">Admin</a>
      <button hx-get="/auth/logout" class="text-white hover:text-gray-300 ml-4">
        Logout
      </button>
      <% } else { %>
      <!-- Usuário não está logado -->
      <a href="/auth/login" class="text-white hover:text-gray-300">Login</a>
      <a href="/auth/register" class="text-white hover:text-gray-300 ml-4"
        >Registrar</a
      >
      <% } %>
    </div>
  </div>
</nav>
